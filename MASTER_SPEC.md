# MASTER SPEC

This file is a pointer, not a duplicate specification. The authoritative specification is split across `docs/` to keep Codex context targeted.

Read `AGENTS.md` then `docs/00_INDEX.md`. Execute `docs/10_IMPLEMENTATION_BACKLOG.md` in order. Architecture decisions are frozen in `docs/09_FIXED_ARCHITECTURE_DECISIONS.md`.

Visual reference: `MASTER_ARCHITECTURE_BOARD.html`. Database blueprint: `schemas/database.dbml`.
